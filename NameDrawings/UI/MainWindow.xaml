<Window
    x:Class="EliteSheets.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EliteSheets"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Jooniste printimine"
    Width="1000"
    Height="700"
    MinWidth="1000"
    MinHeight="700"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    mc:Ignorable="av">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!--  Material Design default theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <!--  Material Design dark theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="/EliteSheets;component/UI/Themes/ElementStyles.xaml" />


            </ResourceDictionary.MergedDictionaries>
            <!--  Custom Checkbox Style with Icons  -->
            <Style x:Key="DatagridCheckboxStyle" TargetType="{x:Type CheckBox}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Border
                                Padding="0"
                                Background="Transparent"
                                Cursor="Hand">
                                <Grid Background="Transparent">
                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                        <!--  Checkbox Icon  -->
                                        <materialDesign:PackIcon
                                            x:Name="CheckboxIcon"
                                            Width="20"
                                            Height="20"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource IconForegroundBrush}"
                                            Kind="CheckboxBlankOutline" />

                                        <!--  Checkbox Text  -->
                                        <TextBlock
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource ForegroundBrush}"
                                            Text="{TemplateBinding Content}" />
                                    </StackPanel>

                                    <!--  Clickable Transparent Overlay  -->
                                    <Border
                                        x:Name="ClickableArea"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Background="Transparent"
                                        Cursor="Hand" />
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <!--  Checked State  -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="CheckboxIcon" Property="Kind" Value="CheckboxMarkedOutline" />
                                    <Setter TargetName="CheckboxIcon" Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style
                x:Key="MultiRowHeaderStyle_NoRightBorder"
                BasedOn="{StaticResource MultiRowHeaderStyle}"
                TargetType="DataGridColumnHeader">
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <!--  removes right border  -->
            </Style>
            <!--  Flat icon button: no default hover/press chrome  -->
            <Style x:Key="FlatIconButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>

                            <!--  OPTIONAL: subtle feedback without “weird” color  -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" Value="0.70" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.35" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent" ClipToBounds="True">
        <!--  Outer Border with Opacity and Blur  -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border
            Grid.RowSpan="2"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="2"
            ClipToBounds="True"
            CornerRadius="10"
            Opacity="0.7"
            SnapsToDevicePixels="True">
            <Border.Effect>
                <BlurEffect KernelType="Gaussian" Radius="2" />
            </Border.Effect>
        </Border>

        <!--  Content Container (no spans)  -->
        <Border
            Margin="2"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <!--  Row 0: Title bar  -->
                    <RowDefinition Height="*" />
                    <!--  Row 1: DataGrid  -->
                    <RowDefinition Height="Auto" />
                    <!--  Row 2: Bottom controls  -->
                </Grid.RowDefinitions>

                <!--  Title bar (Row 0)  -->
                <local:TitleBar
                    Grid.Row="0"
                    Foreground="{DynamicResource ForegroundBrush}"
                    Loaded="TitleBar_Loaded" />

                <!--  Search bar (Row 0)  -->
                <Border
                    Grid.Row="0"
                    Height="24"
                    Margin="0,0,200,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Opacity="0.95">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="28" />
                        </Grid.ColumnDefinitions>

                        <!--  Magnify icon  -->
                        <materialDesign:PackIcon
                            Grid.Column="0"
                            Width="16"
                            Height="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource IconBrush}"
                            Kind="Magnify" />

                        <!--  Search textbox placeholder  -->
                        <TextBlock
                            Grid.Column="1"
                            Margin="2,0,0,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource ForegroundBrush}"
                            IsHitTestVisible="False"
                            Opacity="0.5"
                            Text="Search...">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SheetSearchTextBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!--  Search textbox  -->
                        <TextBox
                            x:Name="SheetSearchTextBox"
                            AutomationProperties.Name="Search sheets"
                            Grid.Column="1"
                            Width="150"
                            Margin="0"
                            VerticalContentAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            CaretBrush="{DynamicResource ForegroundBrush}"
                            FontSize="12"
                            Foreground="{DynamicResource ForegroundBrush}"
                            TextChanged="SheetSearchTextBox_TextChanged"
                            ToolTip="Search (Number / Name / Version / Size)" />

                        <!--  Clear button  -->
                        <Button
                            x:Name="ClearSearchButton"
                            AutomationProperties.Name="Clear search"
                            Grid.Column="2"
                            Width="24"
                            Height="24"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Click="ClearSearchButton_Click"
                            Style="{StaticResource FlatIconButton}"
                            ToolTip="Clear">
                            <materialDesign:PackIcon
                                Width="16"
                                Height="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource IconBrush}"
                                Kind="CloseCircleOutline" />
                        </Button>
                    </Grid>
                </Border>

                <Grid
                    Grid.Row="0"
                    Margin="0,0,100,0"
                    HorizontalAlignment="Right">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  NEW: Reload button (to the left of theme toggle)  -->
                    <Button
                        x:Name="ReloadButton"
                        AutomationProperties.Name="Reload sheets"
                        Grid.Column="0"
                        Margin="0,0,30,0"
                        Click="ReloadButton_Click"
                        Style="{StaticResource ReloadButtonStyle}"
                        ToolTip="Reload">
                        <materialDesign:PackIcon Kind="Refresh" Style="{StaticResource ReloadIconStyle}" />
                    </Button>

                    <!--  Existing theme toggle  -->
                    <ToggleButton
                        x:Name="ThemeToggleButton"
                        AutomationProperties.Name="Toggle Theme"
                        Grid.Column="1"
                        Width="28"
                        Height="15.5"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Click="ToggleTheme_Click"
                        IsChecked="True"
                        ToolTip="Toggle Theme">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                <Grid Background="Transparent">
                                    <materialDesign:PackIcon
                                        x:Name="ThemeToggleIcon"
                                        Width="30"
                                        Height="30"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource IconBrush}"
                                        Kind="ToggleSwitchOffOutline" />
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOffOutline" />
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOutline" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>


                <!--  DataGrid card (Row 1)  -->
                <materialDesign:Card
                    Grid.Row="1"
                    Margin="10,0,10,10"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    UniformCornerRadius="10">
                    <DataGrid
                        x:Name="SheetsDataGrid"
                        AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                        AutoGenerateColumns="False"
                        Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        CanUserResizeRows="False"
                        Foreground="{DynamicResource ForegroundBrush}"
                        GridLinesVisibility="None"
                        HeadersVisibility="Column"
                        ItemsSource="{Binding Sheets}"
                        PreviewMouseLeftButtonDown="SheetsDataGrid_PreviewMouseLeftButtonDown"
                        RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                        RowHeaderWidth="0"
                        RowHeight="20"
                        SelectionMode="Extended"
                        SelectionUnit="FullRow">

                        <DataGrid.Columns>
                            <!--  Checkbox column  -->
                            <DataGridTemplateColumn Width="35" HeaderStyle="{StaticResource MultiRowHeaderStyle}">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <CheckBox
                                                x:Name="CheckAllBox"
                                                AutomationProperties.Name="Select all sheets"
                                                Click="CheckAllBox_Click"
                                                Style="{StaticResource DatagridCheckboxStyle}"
                                                ToolTip="Check/Uncheck All" />
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <CheckBox
                                                Click="Checkbox_Click"
                                                IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource DatagridCheckboxStyle}">
                                                <AutomationProperties.Name>
                                                    <MultiBinding StringFormat="Select sheet {0} - {1}">
                                                        <Binding Path="Number" />
                                                        <Binding Path="Name" />
                                                    </MultiBinding>
                                                </AutomationProperties.Name>
                                            </CheckBox>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!--  Sheet Number  -->
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Number}"
                                Header="Joonise Number"
                                HeaderStyle="{StaticResource MultiRowHeaderStyle}"
                                IsReadOnly="True" />

                            <!--  Sheet Name  -->
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Name}"
                                Header="Joonise nimi"
                                HeaderStyle="{StaticResource MultiRowHeaderStyle}"
                                IsReadOnly="True" />

                            <!--  Versioon (NEW)  -->
                            <DataGridTextColumn
                                Width="75"
                                Binding="{Binding Version}"
                                ElementStyle="{StaticResource CenterAlignedCellStyle}"
                                Header="Versioon"
                                HeaderStyle="{StaticResource MultiRowHeaderStyle}"
                                IsReadOnly="True" />


                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>

                <!--  Bottom controls (Row 2)  -->
                <materialDesign:Card
                    Grid.Row="2"
                    Margin="10,0,10,10"
                    Padding="10"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    UniformCornerRadius="10">
                    <Grid Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Left column: settings and browse  -->
                        <StackPanel
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Orientation="Vertical">
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <CheckBox
                                    x:Name="DwgExportCheckbox"
                                    Margin="0,0,20,0"
                                    Content="DWG"
                                    FontSize="24"
                                    FontWeight="Bold"
                                    IsChecked="True"
                                    Style="{StaticResource CustomCheckboxStyle}" />
                                <CheckBox
                                    x:Name="PdfExportCheckbox"
                                    Margin="0,0,20,0"
                                    Content="PDF"
                                    FontSize="24"
                                    FontWeight="Bold"
                                    IsChecked="True"
                                    Style="{StaticResource CustomCheckboxStyle}" />
                            </StackPanel>

                            <StackPanel
                                Margin="0,5,0,0"
                                HorizontalAlignment="Left"
                                Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{DynamicResource ForegroundBrush}"
                                    Text="DWG Export settings:" />
                                <ComboBox
                                    x:Name="DwgExportComboBox"
                                    Width="100"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    DisplayMemberPath="Name"
                                    Style="{StaticResource DarkThemeComboBox}"
                                    ToolTip="Select DWG Export Setup" />
                            </StackPanel>

                            <StackPanel
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                Orientation="Horizontal">
                                <Button
                                    x:Name="BrowseButton"
                                    Width="80"
                                    Height="25"
                                    Margin="0,0,5,0"
                                    Click="BrowseButton_Click"
                                    Content="Browse"
                                    FontSize="14"
                                    Style="{StaticResource CustomActionButtonStyle}" />
                                <TextBox
                                    x:Name="ExportPathTextBox"
                                    Height="25"
                                    MinWidth="300"
                                    VerticalContentAlignment="Center"
                                    IsReadOnly="True"
                                    Style="{StaticResource CustomTextBoxStyle}"
                                    Text="C:\Path\To\Export" />
                            </StackPanel>
                        </StackPanel>

                        <!--  Right column: print button  -->
                        <StackPanel
                            Grid.Column="1"
                            Margin="10,0,10,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <Button
                                x:Name="PrintButton"
                                Width="200"
                                Height="100"
                                VerticalAlignment="Center"
                                Click="PrintButton_Click"
                                Content="Print"
                                FontSize="24"
                                FontWeight="Bold"
                                Style="{StaticResource CustomActionButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </Border>

        <!--  Left Edge  -->
        <Border
            x:Name="LeftEdge"
            Width="5"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch"
            Background="Transparent"
            Cursor="SizeWE"
            MouseEnter="LeftEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="LeftEdge_MouseLeftButtonDown" />
        <!--  Right Edge  -->
        <Border
            x:Name="RightEdge"
            Width="9"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Background="Transparent"
            Cursor="SizeWE"
            MouseEnter="RightEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="RightEdge_MouseLeftButtonDown" />
        <!--  Bottom Edge  -->
        <Border
            x:Name="BottomEdge"
            Height="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNS"
            MouseEnter="BottomEdge_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomEdge_MouseLeftButtonDown" />
        <!--  Bottom-Left Corner  -->
        <Border
            x:Name="BottomLeftCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNESW"
            MouseEnter="BottomLeftCorner_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomLeftCorner_MouseLeftButtonDown" />
        <!--  Bottom-Right Corner  -->
        <Border
            x:Name="BottomRightCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="Transparent"
            Cursor="SizeNWSE"
            MouseEnter="BottomRightCorner_MouseEnter"
            MouseLeave="Edge_MouseLeave"
            MouseLeftButtonDown="BottomRightCorner_MouseLeftButtonDown" />
    </Grid>
</Window>